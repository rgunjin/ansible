---
  - name: init_skript
    hosts: localhost
    become: true
    tasks:
      - name: install fish
        pacman: name=fish
      - name: change shell
        shell: chsh -s /usr/bin/fish root
        shell: name=fish
