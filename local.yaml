---
  - name: init_skript
    hosts: localhost
    become: true
    pre_tasks:
      - name: update cache
        pacman:
          update_cache: true
    tasks:
      - user:
        name: rasul
        password: "{{ 'myvaultpassword' | password_hash('sha512', 'mypermsalt') }}"
